# Task Plan

## 2026-03-30 Addendum

- Goal: derive an interview-ready technical report from the implemented AI shopping MVP.
- Constraints: use only repository-backed facts; keep the report skimmable within five minutes; prefer tables, charts, and flow diagrams.
- Deliverable: `docs/AI_导购项目技术报告.md`
- Status: `completed`

## Goal

基于 `MVP_PRD.md`，制定 AI 二手 Switch 导购 Agent MVP 的中文执行计划，确保后续工作可以按步骤逐步落地，从需求收敛、接口设计、规则实现到前后端联调和验证形成完整闭环。

## Constraints

- 当前仓库仅存在 PRD，暂无现成代码、接口定义或数据样本。
- MVP 范围仅支持 `Switch` 品类，不扩展到其他商品类型。
- MVP 仅支持少量必要补问，不做复杂多轮导购对话。
- 模型负责需求理解与结构化输出，不直接参与 SQL 或商品检索执行。
- 后端负责检索、过滤、排序、卡片数据组装。
- 前端仅需支持简化版结果卡片展示，不追求完整商业化体验。
- 需要支持接入 LLM 并在本地环境完成端到端运行。

## Success Criteria

- 形成可执行的模型输出 schema。
- 形成可执行的后端检索、过滤、排序规则。
- 形成前后端接口定义与卡片字段协议。
- 打通从自然语言输入到推荐卡片展示的本地 MVP 链路。
- 能用至少一批小样本数据验证主流程、补问逻辑和排序结果是否基本可用。

## Phases

### Phase 1. PRD 拆解与范围冻结

状态: `pending`

目标:
- 将 PRD 中的能力边界、字段定义、模块职责拆成可执行任务。
- 明确 MVP 必做项、可延期项、风险点和默认假设。

执行步骤:
1. 提炼 PRD 中的用户主流程、模型职责、后端职责、前端职责。
2. 冻结 MVP 范围，明确不做项，避免实现过程中范围膨胀。
3. 产出统一术语表，包括版本、成色、功能等级、维修、浸液、配件完整度等定义。
4. 明确每个模块的输入、输出和责任边界。

阶段产出:
- 需求拆解文档
- 模块边界说明
- 术语表

验收标准:
- 团队对 MVP 范围、接口边界、排序目标没有歧义。

### Phase 2. 数据模型与样本准备

状态: `pending`

目标:
- 将 PRD 中的字段定义整理为可供后端和模型共同使用的数据结构。
- 准备一批可用于调试与验证的小样本数据。

执行步骤:
1. 将商品字段、外观查验、功能查验、拆修浸液、核心配件、扩展配件整理为标准数据模型。
2. 明确字段类型、可选值、空值策略、枚举值和默认值。
3. 设计一份最小可用商品数据样本，覆盖不同版本、价格、成色、维修、配件情况。
4. 设计一份检验报告样本，确保风险字段能支撑排序和解释。
5. 准备用户查询样本，覆盖预算明确、条件缺失、强硬约束、偏好型约束等典型场景。

阶段产出:
- 商品数据 schema
- 检验报告 schema
- 样本数据集
- 示例用户 query 集

验收标准:
- 样本足够覆盖主流程、补问逻辑和排序逻辑。

### Phase 3. 模型输出协议设计

状态: `pending`

目标:
- 定义稳定的模型输出结构，确保模型结果可以被后端可靠消费。

执行步骤:
1. 设计模型输出 schema，至少包含 `category`、`filters`、`user_intent`、`parse_explanation`、`clarification_needed`。
2. 为 `filters` 中的每个字段定义类型、含义、允许范围和示例。
3. 定义补问触发规则，明确哪些字段缺失时允许发起补问。
4. 设计补问响应后的二次解析协议，避免状态丢失。
5. 制定提示词模板，约束模型稳定输出 JSON 或等价结构化格式。
6. 用用户样本对 schema 和提示词做一次人工走查。

阶段产出:
- 模型输出 schema 文档
- 提示词草案
- 补问规则说明

验收标准:
- 任意一条合规用户输入都能映射到稳定结构化结果。

### Phase 4. 后端过滤与排序规则设计

状态: `pending`

目标:
- 把 PRD 的业务规则转成可执行的后端逻辑。

执行步骤:
1. 实现硬过滤规则，包括预算、版本、区域、最低等级、维修接受度、浸液接受度、配件约束等。
2. 定义综合排序公式，将匹配度、风险、性价比、配件完整度拆成可计算子分数。
3. 设计风险评分规则，综合外观、功能、维修、浸液信息。
4. 设计性价比基线规则，即使初期仅使用启发式方法，也要写清计算依据。
5. 设计推荐理由生成所需的证据字段。
6. 设计风险提示生成所需的触发条件和文案模板。

阶段产出:
- 后端过滤规则文档
- 排序规则文档
- 推荐理由与风险提示规则

验收标准:
- 规则可直接映射为代码实现，且支持解释为什么推荐或不推荐。

### Phase 5. 服务接口与模块骨架实现

状态: `pending`

目标:
- 建立后端服务最小骨架，打通模型调用、规则执行和结果返回。

执行步骤:
1. 创建服务目录结构，拆分为模型层、规则层、数据层、接口层。
2. 实现一个接收自然语言输入的查询接口。
3. 实现模型解析模块，先支持 mock，再切换真实 LLM。
4. 实现结构化过滤器到检索条件的映射器。
5. 实现商品过滤、排序和卡片数据组装逻辑。
6. 实现错误处理和兜底逻辑，例如模型输出缺失、无结果、补问触发。

阶段产出:
- 后端最小可运行服务
- 查询接口
- 过滤排序模块
- 卡片组装模块

验收标准:
- 后端在无前端参与的情况下，可返回结构正确的推荐结果。

### Phase 6. 前端 MVP 展示实现

状态: `pending`

目标:
- 提供最小可用前端页面，完成输入、补问、结果展示三个核心交互。

执行步骤:
1. 搭建前端页面骨架，包括需求输入区、补问区、结果区。
2. 接入后端查询接口和补问流程。
3. 实现简化卡片组件，展示标题、价格、基础摘要、检验摘要、配件摘要、推荐理由、风险提示。
4. 实现无结果、加载中、解析失败等状态展示。
5. 优化信息层级，确保用户能快速理解推荐理由和风险点。

阶段产出:
- 前端 MVP 页面
- 商品推荐卡片组件
- 补问交互组件

验收标准:
- 用户能在一个页面内完成输入、查看结果、理解推荐理由。

### Phase 7. 本地 LLM 接入与端到端联调

状态: `pending`

目标:
- 用真实 LLM 替代 mock 流程，完成本地端到端链路联调。

执行步骤:
1. 确定本地 LLM 接入方式、模型参数和调用接口。
2. 完成后端对 LLM 的封装，支持超时、重试、日志记录。
3. 联调主流程，验证用户输入到结构化过滤器输出是否稳定。
4. 联调补问流程，验证补问前后上下文是否能正确衔接。
5. 联调结果卡片展示，检查字段映射是否完整。

阶段产出:
- LLM 接入配置
- 联调记录
- 可运行的本地端到端 MVP

验收标准:
- 从自然语言输入到推荐卡片展示的整条链路可稳定运行。

### Phase 8. 小样本验证与迭代

状态: `pending`

目标:
- 用小样本验证规则和体验是否达到 MVP 预期。

执行步骤:
1. 设计验证用例，覆盖明确需求、模糊需求、条件冲突、极端预算、风险偏好差异等情形。
2. 记录每条用例的解析结果、补问情况、最终排序结果和人工判断。
3. 检查推荐理由是否可信，风险提示是否充分。
4. 根据结果回调 schema、规则权重、补问阈值和前端展示。
5. 整理剩余问题清单，为下一轮迭代提供依据。

阶段产出:
- 验证用例集
- 验证结果记录
- 迭代问题清单

验收标准:
- 主场景用例通过，主要问题可归因并可继续迭代。

## Current Status

- `completed`: 已完成 PRD 阅读与执行计划初稿整理。
- `in_progress`: 正在沉淀项目级计划文件，作为后续实现工作的执行基线。
- `pending`: 代码实现、接口定义、样本构建与联调均尚未开始。

## Risks / Blockers

- 当前缺少真实或模拟商品数据，排序与推荐解释暂时只能先按规则设计推进。
- “性价比”缺少明确行情基线，初期需要采用启发式规则，后续再补充市场参考。
- 补问触发阈值如果设计不当，可能影响转化率和交互流畅性。
- 如果模型结构化输出不稳定，会直接影响后端规则执行和联调效率。

## Recommended Execution Order

建议严格按以下顺序推进，避免返工：

1. 先完成 Phase 1，冻结范围与术语。
2. 再完成 Phase 2，统一数据模型和样本。
3. 接着完成 Phase 3，锁定模型输出协议。
4. 然后完成 Phase 4，固化过滤与排序规则。
5. 再进入 Phase 5，搭后端最小骨架。
6. 然后完成 Phase 6，接前端展示。
7. 再推进 Phase 7，接入真实 LLM 做联调。
8. 最后执行 Phase 8，做小样本验证和迭代。
