# AI 二手 Switch 导购 Agent MVP PRD

## 1. 文档目标

本文档定义 AI 二手 Switch 导购 Agent 的 MVP 范围、目标用户、核心场景、产品流程、数据基础、排序逻辑、接口边界与验证重点，用于后续产品、模型、后端和前端协作。

## 2. 产品概述

### 2.1 一句话定义

这是一个面向普通消费者的 AI 二手 Switch 导购 Agent。它通过理解用户的自然语言购买需求，并结合平台商品字段和检验报告，对候选商品进行筛选、排序和解释，帮助用户更安心地做出购买决策。

### 2.2 产品目标

- 降低用户在二手 Switch 购买中的信息理解成本
- 将自然语言需求转化为可执行的筛选范围
- 基于平台检验能力输出更可信的推荐理由和风险提示
- 提升用户从搜索到商品详情的决策效率

### 2.3 MVP 范围

- 仅支持 `Switch` 一个品类
- 支持自然语言需求输入
- 支持少量必要补问
- 支持结构化筛选范围输出
- 支持后端检索、过滤、排序
- 支持简化版商品卡片展示
- 支持接入 LLM 后在本地环境完成端到端运行

### 2.4 非目标范围

- 不支持 Switch 以外品类
- 不做复杂长期多轮导购对话
- 不把图片理解作为 MVP 主能力
- 不做卖家信用体系和卖家画像
- 不做自动议价、自动下单、全网比价
- 不做跨会话用户偏好学习

## 3. 目标用户

### 3.1 核心用户画像

- 想买二手 Switch 的普通消费者
- 愿意接受二手，但缺乏机器版本、成色、维修风险的判断能力
- 不愿意逐条阅读复杂商品信息和检验报告
- 更关注“买得安心”，而不仅仅是“买得便宜”

### 3.2 核心痛点

- 不知道如何把购买意图准确转换成筛选条件
- 看不懂检验报告，不知道哪些信息最重要
- 面对多个候选商品时难以比较哪个更适合自己

## 4. 核心使用场景

### 4.1 主场景

用户通过一句自然语言描述需求，例如：

`想买一台 1600 以内的 Switch OLED，成色好一点，不要维修机，日版也可以，最好配件齐全。`

系统完成以下动作：

1. 通过LLM解析预算、版本、区域、成色、维修接受度、配件要求等关键信息
2. 在必要时补问少量关键缺失信息
3. 生成结构化筛选范围
4. 由后端根据筛选范围执行检索和过滤
5. 对候选商品进行综合排序
6. 以卡片形式展示推荐理由和风险提示

## 5. 产品流程

### 5.1 主流程

1. 用户输入自然语言需求
2. LLM执行需求解析
3. LLM输出结构化筛选范围
4. 后端接收筛选范围并执行检索
5. 后端执行硬过滤和排序
6. 后端组装卡片数据
7. 前端展示卡片结果

### 5.2 补问原则

- 仅在关键信息缺失且明显影响推荐结果时补问
- MVP 阶段最多补问 1 到 3 个问题
- 优先补问以下高影响字段：
  - 预算
  - 版本偏好
  - 是否接受维修或浸液
  - 是否要求核心配件齐全

## 6. 核心能力定义

### 6.1 自然语言需求解析

模型需要从用户输入中提取：

- 预算范围
- 版本偏好
- 区域偏好
- 内存要求
- 颜色偏好
- 外观要求
- 功能要求
- 是否接受维修
- 是否接受浸液
- 是否要求核心配件齐全
- 是否要求 Joy-Con 完整
- 是否要求箱说
- 使用场景
- 风险偏好

### 6.2 筛选范围输出

模型输出结果不直接参与检索执行，而是输出一个稳定的结构化筛选范围，由后端解析后执行。

### 6.3 后端检索与过滤

后端根据结构化筛选范围：

- 执行商品召回
- 进行硬条件过滤
- 计算排序分数
- 返回卡片展示数据

### 6.4 商品卡片输出

卡片仅保留决策核心信息，字段包括：

- 商品标题
- 价格
- 基础属性摘要
- 检验摘要
- 配件摘要
- 推荐理由
- 风险提示

## 7. 数据设计

本节以用户确认的一级字段为基础，补全二级字段。

### 7.1 产品字段

- `product_id`
- `title`
- `category`
- `version`
  - `model`: `standard | oled | lite`
  - `edition_type`: `regular | limited`
  - `edition_name`
- `memory`
  - `capacity_gb`
  - `is_expandable`
- `region`
  - `market`
  - `region_code`
- `color`
  - `body_color`
  - `joycon_left_color`
  - `joycon_right_color`
  - `special_colorway`
- `price`
  - `sale_price`
  - `currency`

### 7.2 外观查验

- `grade`
- `overall_condition`
- `has_visible_flaws`
- `flaw_locations`
- `flaw_details`
  - `location`
  - `type`
  - `severity`
  - `description`

### 7.3 功能查验

- `grade`
- `sound_function`
  - `status`
  - `details`
- `button_function`
  - `status`
  - `details`
- `screen_display`
  - `status`
  - `details`
- `other_functions`
  - `status`
  - `details`
- `function_summary`

### 7.4 拆修浸液

- `liquid_damage`
  - `status`
  - `details`
- `repair_status`
  - `status`
  - `details`
- `disassembly_status`
  - `status`
  - `details`
- `risk_level`

### 7.5 核心配件

- `overall_status`
- `charger`
  - `included`
  - `original`
  - `status`
- `dock`
  - `included`
  - `original`
  - `status`
- `grip`
  - `included`
  - `original`
  - `status`
- `joycon`
  - `left_included`
  - `right_included`
  - `pair_complete`
  - `status`

### 7.6 配件

- `joycon_pair_count`
- `extra_accessories`
  - `name`
  - `included`
  - `status`
  - `description`
- `accessory_summary`

## 8. 模型与后端边界

### 8.1 模型职责

- 理解用户自然语言需求
- 提取关键约束
- 输出结构化筛选范围
- 生成解析说明
- 在必要时发起少量补问

### 8.2 后端职责

- 解析模型输出的筛选范围
- 映射为检索与过滤条件
- 执行硬过滤
- 计算排序分数
- 生成卡片所需摘要字段
- 返回推荐理由与风险提示所需的数据基础

### 8.3 输出原则

- 模型不直接输出 SQL
- 模型不直接返回商品结果
- 检索、过滤、排序由后端负责

## 9. 排序逻辑

### 9.1 排序总原则

先过滤硬条件，再按 `匹配度 + 风险 + 性价比 + 配件完整度` 综合排序。

### 9.2 硬过滤

建议作为硬过滤的条件包括：

- 预算
- 版本
- 区域
- 最低外观等级
- 最低功能等级
- 是否接受维修
- 是否接受浸液
- 是否要求核心配件齐全
- 是否要求 Joy-Con 成对
- 是否要求箱说

### 9.3 综合排序因子

- `匹配度`
  - 是否符合预算、版本、区域、场景、成色偏好
- `风险`
  - 外观查验、功能查验、拆修浸液的综合风险
- `性价比`
  - 商品价格与状态、版本、配件完整度是否匹配
- `配件完整度`
  - 是否满足用户对关键配件的要求

### 9.4 排序原则补充

- 用户明确表达的硬约束优先级最高
- 满足硬约束后，优先推荐低风险商品
- 在风险接近时，再提升性价比权重

## 10. 卡片结构

### 10.1 卡片字段

- `title`
- `price`
- `base_summary`
- `inspection_summary`
- `accessory_summary`
- `recommend_reasons`
- `risk_warnings`

### 10.2 字段说明

- `base_summary`
  - 版本 / 区域 / 颜色 / 内存
- `inspection_summary`
  - 外观等级 / 功能等级 / 是否维修 / 是否浸液
- `accessory_summary`
  - 核心配件是否齐全 / Joy-Con 是否完整 / 箱说情况
- `recommend_reasons`
  - 从匹配度、低风险、配件完整度等角度说明为什么推荐
- `risk_warnings`
  - 明确提醒仍需关注的不足或不确定项

## 11. 关键接口方向

### 11.1 模型输出接口方向

模型输出应包含：

- `category`
- `filters`
- `user_intent`
- `parse_explanation`
- `clarification_needed`

### 11.2 后端返回前端的数据方向

后端返回应包含：

- 简化卡片结构所需字段
- 排序结果
- 推荐理由
- 风险提示

## 12. 成功指标

建议重点观察：

- 用户筛选完成时长是否缩短
- 商品详情页点击率是否提升
- 推荐结果进入详情页的转化率是否提升
- 加购或下单转化是否提升
- 用户是否更少重复修改筛选条件

## 13. 风险与待验证点

- 用户是否愿意用自然语言表达明确需求
- 字段是否足够支撑“安心购买决策”的解释
- 性价比判断是否需要额外行情基线
- 补问数量对转化率的影响
- 用户是否信任推荐理由与风险提示

## 14. MVP 交付建议

建议后续进入以下产出物：

1. 模型输出 schema 文档
2. 后端筛选与排序规则文档
3. 本地 LLM 接入与运行文档
4. 前端卡片接口文档
5. 小样本本地验证方案
